import styled from "styled-components";
import { DEVICE_BREAKPOINTS } from "../../utils/device";

export const Container = styled.div`
    max-width: ${DEVICE_BREAKPOINTS.XL}px;
    margin: auto;
    padding: 10px;
`